import { SearchCourcePipe } from './search-cource.pipe';

describe('SearchCourcePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCourcePipe();
    expect(pipe).toBeTruthy();
  });
});
